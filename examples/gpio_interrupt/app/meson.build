project('gpio-interrupt', 'c',
  default_options : [
    'b_pie=false',
    'default_library=static',
  ],
)

src = files('main.c')

executable('gpio_interrupt',
           src,
           install : false,
)
