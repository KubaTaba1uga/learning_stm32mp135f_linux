project('btn-interrupt', 'c',
  default_options : [
    'b_pie=false',
    'default_library=static',
  ],
)

src = files('main.c')

executable('btn_interrupt',
           src,
           install : false,
)
